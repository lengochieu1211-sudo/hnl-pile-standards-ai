import fs from 'node:fs';
import crypto from 'node:crypto';
import { spawnSync } from 'node:child_process';

const read=p=>fs.readFileSync(p,'utf8');
const write=(p,s)=>{fs.mkdirSync(p.split('/').slice(0,-1).join('/')||'.',{recursive:true});fs.writeFileSync(p,s,'utf8');};
const sha=p=>crypto.createHash('sha256').update(fs.readFileSync(p)).digest('hex');
const must=(ok,msg)=>{if(!ok) throw new Error(msg);};
const run=(cmd,args)=>{const r=spawnSync(cmd,args,{stdio:'inherit',shell:false});if(r.status!==0) process.exit(r.status??1);};

const routerPath='src/engineering-router.js';
let router=read(routerPath);
const oldBlock=`function calcStatic10304(q='') {\n  const Ru=explicit(q,['Ru,k','R_u,k','Ruk','Ru'],'(?:kN)?'); const gammaCg1=explicit(q,['gamma_cg1','γc,g1']) ?? 1; const gammaC=explicit(q,['gamma_c','γc']) ?? 1;\n  if(Ru==null) return {ok:false,missing:['Ru,k sức chịu tải giới hạn từ đường cong tải-lún/thí nghiệm (kN)']};\n  const Rk=gammaC*Ru/gammaCg1; return {ok:true,RkKn:Rk,inputs:{Ru,gammaCg1,gammaC},steps:[\`CT (20): Rk=γc·Ru,k/γc,g1=\${Rk.toFixed(3)} kN.\`],provenance:['TCVN 10304:2025 · 7.3.2.1-7.3.2.3 · CT (20),(21) · tr.49-50']};\n}`;
const newBlock=`function parseStaticLoadCurve10304(q='') {\n  const points=[]; const re=/(\\d+(?:[.,]\\d+)?)\\s*kN\\s*(?:@|:|\\/|->|→)\\s*(\\d+(?:[.,]\\d+)?)\\s*mm/gi; let m;\n  while((m=re.exec(String(q)))) points.push({loadKn:number(m[1]),settlementMm:number(m[2])});\n  points.sort((a,b)=>a.settlementMm-b.settlementMm);\n  return points;\n}\nfunction interpolateStaticLoadAtSettlement10304(points,targetMm){\n  if(!Array.isArray(points)||points.length<2) return {ok:false,reason:'Cần ít nhất 2 điểm tải–lún dạng 500kN@10mm.'};\n  for(let i=1;i<points.length;i++){\n    if(!(points[i].settlementMm>points[i-1].settlementMm)) return {ok:false,reason:'Độ lún các điểm thử phải tăng nghiêm ngặt.'};\n    if(points[i].loadKn+1e-9<points[i-1].loadKn) return {ok:false,reason:'Tải trọng đường cong nén phải không giảm theo độ lún.'};\n  }\n  if(targetMm<points[0].settlementMm-1e-9||targetMm>points.at(-1).settlementMm+1e-9) return {ok:false,reason:\`Độ lún mục tiêu \${targetMm.toFixed(3)} mm nằm ngoài phạm vi đường cong \${points[0].settlementMm}–\${points.at(-1).settlementMm} mm; không được ngoại suy.\`};\n  const exact=points.find(p=>Math.abs(p.settlementMm-targetMm)<=1e-9); if(exact) return {ok:true,loadKn:exact.loadKn,mode:'EXACT'};\n  for(let i=1;i<points.length;i++){ const a=points[i-1],b=points[i]; if(targetMm>a.settlementMm&&targetMm<b.settlementMm){ const t=(targetMm-a.settlementMm)/(b.settlementMm-a.settlementMm); return {ok:true,loadKn:a.loadKn+t*(b.loadKn-a.loadKn),mode:'LINEAR_INTERPOLATION',bracket:[a,b]}; }}\n  return {ok:false,reason:'Không xác định được tải tại độ lún mục tiêu.'};\n}\nfunction calcStatic10304(q='') {\n  let Ru=explicit(q,['Ru,k','R_u,k','Ruk','Ru'],'(?:kN)?'); const gammaCg1=explicit(q,['gamma_cg1','γc,g1']) ?? 1; const gammaC=explicit(q,['gamma_c','γc']) ?? 1;\n  let staticCriterion=null;\n  if(Ru==null){\n    const suMt=explicit(q,['su,mt','su_mt','s_u,mt','s_u_mt','su mt'],'(?:mm)?');\n    const IL=explicit(q,['IL','I_L']); let zeta=explicit(q,['zeta','ζ']);\n    if(zeta==null&&IL!=null) zeta=IL>0.5?0.2:0.35;\n    const points=parseStaticLoadCurve10304(q);\n    const missing=[]; if(!(suMt>0)) missing.push('su,mt độ lún giới hạn trung bình (mm)'); if(!(zeta>0)) missing.push('ζ hoặc IL để chọn ζ=0,2/0,35'); if(points.length<2) missing.push('ít nhất 2 điểm đường cong tải–lún, ví dụ 500kN@10mm');\n    if(missing.length) return {ok:false,status:'REVIEW',missing};\n    const targetSettlementMm=Math.min(zeta*suMt,40); const interp=interpolateStaticLoadAtSettlement10304(points,targetSettlementMm);\n    if(!interp.ok) return {ok:false,status:'REVIEW',inputs:{suMt,IL,zeta,targetSettlementMm,curve:points},missing:[interp.reason]};\n    Ru=interp.loadKn; staticCriterion={suMt,IL,zeta,targetSettlementMm,curve:points,interpolation:interp.mode,bracket:interp.bracket||null};\n  }\n  const Rk=gammaC*Ru/gammaCg1;\n  const steps=[]; if(staticCriterion) steps.push(\`CT (21): s= min(ζ·su,mt;40)=min(\${staticCriterion.zeta}·\${staticCriterion.suMt};40)=\${staticCriterion.targetSettlementMm.toFixed(3)} mm → Ru,k=\${Ru.toFixed(3)} kN (\${staticCriterion.interpolation}).\`);\n  steps.push(\`CT (20): Rk=γc·Ru,k/γc,g1=\${Rk.toFixed(3)} kN.\`);\n  return {ok:true,status:'VERIFIED',staticMode:staticCriterion?'CURVE_CT21':'DIRECT_RU_CT20',formulaId:staticCriterion?21:20,RkKn:Rk,inputs:{Ru,gammaCg1,gammaC,...(staticCriterion||{})},steps,provenance:['TCVN 10304:2025 · 7.3.2.1-7.3.2.3 · CT (20),(21) · tr.49-50','CT (21): Ru lấy tại s=ζ·su,mt, khống chế s≤40 mm; nội suy tuyến tính chỉ trong miền đường cong đo được']};\n}`;
must(router.includes(oldBlock),'Static block baseline mismatch');
router=router.replace(oldBlock,newBlock); write(routerPath,router);

const tests=`import test from 'node:test';\nimport assert from 'node:assert/strict';\nimport { solveEngineeringQuestion, engineeringExcelPayload } from '../src/engineering-router.js';\nconst close=(a,b,t=1e-6)=>assert.ok(Math.abs(a-b)<=t,\`${'${a}'} != ${'${b}'}\`);\n\ntest('P5.3 keeps direct CT20 Ru input unchanged',()=>{ const x=solveEngineeringQuestion('Thử tải tĩnh Ru,k=1200 kN gamma_cg1=1 gamma_c=1'); assert.equal(x.result.ok,true); assert.equal(x.result.staticMode,'DIRECT_RU_CT20'); close(x.result.RkKn,1200); });\ntest('P5.3 CT21 weak soil IL>0.5 selects zeta 0.2 exact point',()=>{ const x=solveEngineeringQuestion('Thử tải tĩnh IL=0.7 su,mt=100 mm curve 0kN@0mm; 500kN@10mm; 1000kN@20mm; 1300kN@30mm'); assert.equal(x.result.ok,true); assert.equal(x.result.staticMode,'CURVE_CT21'); close(x.result.inputs.zeta,0.2); close(x.result.inputs.targetSettlementMm,20); close(x.result.RkKn,1000); });\ntest('P5.3 CT21 stiff soil uses zeta 0.35 and linear interpolation',()=>{ const x=solveEngineeringQuestion('Thử tải tĩnh IL=0.2 su,mt=60 mm 0kN@0mm; 1000kN@20mm; 1200kN@30mm'); assert.equal(x.result.ok,true); close(x.result.inputs.targetSettlementMm,21); close(x.result.RkKn,1020); assert.equal(x.result.inputs.interpolation,'LINEAR_INTERPOLATION'); });\ntest('P5.3 CT21 caps target settlement at 40 mm',()=>{ const x=solveEngineeringQuestion('Thử tải tĩnh IL=0.2 su,mt=200 mm 0kN@0mm; 1000kN@20mm; 1500kN@40mm; 1700kN@50mm'); assert.equal(x.result.ok,true); close(x.result.inputs.targetSettlementMm,40); close(x.result.RkKn,1500); });\ntest('P5.3 CT21 refuses extrapolation beyond measured curve',()=>{ const x=solveEngineeringQuestion('Thử tải tĩnh IL=0.2 su,mt=100 mm 0kN@0mm; 800kN@20mm; 1000kN@30mm'); assert.equal(x.result.ok,false); assert.equal(x.result.status,'REVIEW'); assert.ok(x.result.missing.some(v=>v.includes('không được ngoại suy'))); });\ntest('P5.3 static export gate opens only for successful deterministic result',()=>{ const good=engineeringExcelPayload('Thử tải tĩnh IL=0.7 su,mt=100 mm 0kN@0mm; 500kN@10mm; 1000kN@20mm'); assert.equal(good.canExport,true); const bad=engineeringExcelPayload('Thử tải tĩnh IL=0.2 su,mt=100 mm 0kN@0mm; 800kN@20mm'); assert.equal(bad.canExport,false); });\n`;
write('tests/p5-static-load-promotion.test.mjs',tests);

let count=read('tools/pass83-test-count-gate.mjs');
count=count.replace('// P5.2 CPT promotion adds 8 additive CT25-29/table/applicability/export tests.\n// Never lower any certified component and never skip tests. Final expected suite = 613/613/0.','// P5.2 CPT promotion adds 8 additive CT25-29/table/applicability/export tests.\n// P5.3 static-load promotion adds 6 additive CT20/CT21/curve/applicability/export tests.\n// Never lower any certified component and never skip tests. Final expected suite = 619/619/0.');
count=count.replace('const CPT_P5_ADDITIVE_TESTS = 8;\nconst EXPECTED_TESTS = BASELINE_TESTS + P4_ADDITIVE_TESTS + SPT_GOLDEN_ADDITIVE_TESTS + CPT_P5_ADDITIVE_TESTS;','const CPT_P5_ADDITIVE_TESTS = 8;\nconst STATIC_P5_ADDITIVE_TESTS = 6;\nconst EXPECTED_TESTS = BASELINE_TESTS + P4_ADDITIVE_TESTS + SPT_GOLDEN_ADDITIVE_TESTS + CPT_P5_ADDITIVE_TESTS + STATIC_P5_ADDITIVE_TESTS;');
count=count.replace('`cptP5Additive=${CPT_P5_ADDITIVE_TESTS}`','`cptP5Additive=${CPT_P5_ADDITIVE_TESTS}`,`staticP5Additive=${STATIC_P5_ADDITIVE_TESTS}`');
count=count.replace('+ CPT P5 ${CPT_P5_ADDITIVE_TESTS}`);','+ CPT P5 ${CPT_P5_ADDITIVE_TESTS} + Static P5 ${STATIC_P5_ADDITIVE_TESTS}`);');
write('tools/pass83-test-count-gate.mjs',count);

const finalWorkflow=`name: P5 Static Load Promotion Golden\non:\n  pull_request:\n    branches: [ main ]\n  push:\n    branches: [ 'p5/static-load-promotion' ]\njobs:\n  static-load-promotion:\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v5\n      - uses: actions/setup-node@v5\n        with:\n          node-version: 22\n          cache: npm\n      - run: npm ci\n      - name: Version identity\n        run: npm run check:version\n      - name: Search Brain lock\n        run: npm run check:search\n      - name: Static load CT20-21 focused Golden\n        run: node --test tests/p5-static-load-promotion.test.mjs\n      - name: Exact additive test-count gate\n        run: npm run gate:pass83-tests\n      - name: Full regression suite\n        run: npm test\n`;
write('.github/workflows/p5-static-load-golden.yml',finalWorkflow);

run(process.execPath,['--test','tests/p5-static-load-promotion.test.mjs']);

let sync=read('tools/pass83-source-sync-gate.mjs');
sync=sync.replace('refreshed through P5.2 CPT promotion.','refreshed through P5.3 static-load promotion.');
sync=sync.replace("'src/engineering-router.js':'236442d07a8ade6303e6e9aa0038b083776ad1cc13c8282e77dfdae7cdaf54eb'",`'src/engineering-router.js':'${sha(routerPath)}'`);
sync=sync.replace('P5.2 CPT-certified integration source synchronized','P5.3 static-load-certified integration source synchronized');
write('tools/pass83-source-sync-gate.mjs',sync);

let manifest=JSON.parse(read('RELEASE_SYNC_MANIFEST.json'));
manifest.files['src/engineering-router.js']=sha(routerPath);
manifest.files['tools/pass83-source-sync-gate.mjs']=sha('tools/pass83-source-sync-gate.mjs');
manifest.files['tools/pass83-test-count-gate.mjs']=sha('tools/pass83-test-count-gate.mjs');
manifest.files['tests/p5-static-load-promotion.test.mjs']=sha('tests/p5-static-load-promotion.test.mjs');
manifest.files['.github/workflows/p5-static-load-golden.yml']=sha('.github/workflows/p5-static-load-golden.yml');
write('RELEASE_SYNC_MANIFEST.json',JSON.stringify(manifest,null,2)+'\n');

run(process.execPath,['tools/pass83-source-sync-gate.mjs']);
run(process.execPath,['tools/release-sync-gate.mjs']);
run(process.execPath,['tools/pass83-test-count-gate.mjs']);

fs.rmSync('scripts/p5-static-bootstrap.mjs');
fs.rmSync('.github/workflows/p5-static-bootstrap.yml');
run('git',['config','user.name','github-actions[bot]']); run('git',['config','user.email','41898282+github-actions[bot]@users.noreply.github.com']);
run('git',['add','-A']); run('git',['commit','-m','feat(p5.3): certify static load CT20-21 curve criterion']); run('git',['push','origin','HEAD:p5/static-load-promotion']);
