name: HNL SPT Excel Golden Hardening
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  status:
    runs-on: ubuntu-latest
    steps:
      - run: echo "SPT Golden workflow is being finalized; no branch auto-run during controlled hardening."
