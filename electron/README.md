# HNL Desktop AI shell

Electron starts the bundled HNL Bridge automatically, checks Ollama, and opens the UI on `http://127.0.0.1:8787`.
The renderer keeps Node integration disabled. Local AI calls go through the local Bridge.
