# Upload HNL v1.25.6 bằng GitHub Desktop

1. Giải nén ZIP v1.25.6.
2. Copy toàn bộ nội dung bên trong vào thư mục repo local và cho phép ghi đè.
3. Giữ `package-lock.json` hiện có nếu GitHub Desktop repo đã có; workflow sẽ tự đồng bộ lock từ version cũ sang 1.25.6.
4. Commit: `HNL v1.25.6 - Engineering Symbol Formula Normalizer`.
5. Push origin.
6. Chờ Final Gate → Web → Windows.

Golden cần thử tay: dán `A = A_p = 0,09 m²`, `300 × 300 mm`, `q_b`, `f_i`, `γ_{R,f}`, `R_{bt,ser}`, `N_{d,\max}` vào Hỏi đáp và tab Tính.
