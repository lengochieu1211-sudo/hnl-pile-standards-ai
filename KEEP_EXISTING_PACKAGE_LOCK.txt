QUAN TRỌNG: Không xóa/ghi đè package-lock.json đang có trên nhánh main.
Pass 8.3 thay package.json chỉ để bổ sung các lệnh Golden/gate; dependencies/devDependencies không đổi.
CI bắt buộc chạy npm ci với package-lock.json hiện tại của repo (v1.25.7).
